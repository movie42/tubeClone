mixin videoBlock(video={})
    .video_container
        a(href=routes.videoDetail(video.id))
            video(src=`/${video.fileUrl}`, controls)
            .video_infrom_container
                h3=video.title
                p=video.views
