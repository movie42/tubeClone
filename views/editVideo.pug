extends layouts/layout

block content 
    form(action=`/video${routes.editVideo(video.id)}`, method="post")
        input(type="text", name="title", required=true, value=video.title)
        textarea(placeholder="내용을", name="description")=video.description
        input(type="submit", value="업로드")
    a(href=`/video${routes.deleteVideo(video.id)}`) 비디오 삭제